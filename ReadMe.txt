
# Before open the TensorBoard,
There exist the two files of each model.
One is for training and the other is on testing.
Since If you want to watch the validation error graph while training, move the latest file and open the tensor board.

# Task: classification 

1. By scratch model(Base CNN)
2. By fine-tuning(ResNet50)